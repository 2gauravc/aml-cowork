{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "individual.schema",
  "title": "Individual Schema",
  "description": "Reusable individual entity schema used across KYC documents and cases.",
  "type": "object",

  "properties": {
    "individual_id": {
      "type": "string",
      "pattern": "^IND[0-9]{3}$",
      "description": "System generated unique identifier for the individual."
    },
    "full_name": {
      "type": "string"
    },
    "date_of_birth": {
      "type": "string",
      "format": "date"
    },
    "nationality": {
      "$ref": "../primitives/country.schema.json"
    },
    "residential_address": {
      "type": "string",
      "pattern": "^ADD[0-9]{3}$",
      "description": "Address ID referencing an address entity."
    }
  },

  "required": [
    "individual_id",
    "full_name",
    "date_of_birth",
    "nationality"
  ]
}
